-module(bop_server_log).
-behaviour(application).

log_handle(Type, List) -> % handle log information in
  'todo'.

log_accumulate(Log, Severity) -> % process log information on server
  'todo'.

log_reduce(Loglist) -> % simplify log messages to be useful
  'todo'.

log_out(Loglist) -> % show all log information usefully
  'todo'.
